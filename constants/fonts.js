/**
 * App typography uses the platform system font (SF Pro on iOS, Roboto on Android).
 * Custom Overused Grotesk is not loaded; assets/fonts/OverusedGrotesk-VF.ttf is unused.
 */
export const USE_CUSTOM_FONT = false;
