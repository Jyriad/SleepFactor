/** Re-export total steps for layouts that still import from here */
export { ONBOARDING_TOTAL_STEPS as ONBOARDING_STEP_TOTAL } from './onboardingProgress';

/** Health lab: first sync window (fast path). Fuller history fills in background or via second pass if empty here. */
export const ONBOARDING_HEALTH_LAB_QUICK_DAYS = 14;

export const ONBOARDING_HEALTH_LAB_FULL_DAYS = 30;
